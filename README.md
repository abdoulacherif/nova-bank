# Kontak App — Kotlin Multiplatform

Application native Android + iOS, interface 100% Compose Multiplatform
(pas de WebView), connectée au même backend que le site
(`kontaks.vercel.app/api/*`) — aucune clé Supabase dans l'app, tout passe
par notre serveur, exactement comme le site web.

## Structure